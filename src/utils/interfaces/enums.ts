export enum ESKILL_LEVEL {
  JUNIOR = 1,
  MIDDLE,
  SENIOR,
}

export enum EQUESTION_AMOUNT {
  TEN = 10,
  FIFTEEN = 15,
  TWENTY = 20,
}

export enum ETEST_SPEC {
  FRONT = 1,
  BACK,
  QA,
}

export enum EUSER_ACTION_TYPE {
  TEST = 'TEST',
  INTERVIEW = 'INTERVIEW',
  RESUME_CHECK = 'RESUME_CHECK',
  RESUME_CREATE = 'RESUME_CREATE',
}
